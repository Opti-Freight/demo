name: optifreight
description: "Federated Freight Network"

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.0
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  easy_dashboard: ^0.0.2
  truncate: ^3.0.1
  get: ^4.6.6
  font_awesome_flutter: ^10.7.0
  flutter_svg: ^2.0.10+1
  flutter_svg_provider: ^1.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/images/